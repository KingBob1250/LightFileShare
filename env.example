# 文件服务器环境变量配置
# 复制此文件为 .env 并根据部署环境修改配置

# 服务器配置
PORT=5000
HOST=0.0.0.0

# 管理员密码
ADMIN_PASSWORD=admin123

# 数据库配置
# 数据库文件将存储在 ./db/database.db
DATABASE_URL=sqlite:///db/database.db
# 容器内数据库目录路径（DATABASE_PATH会自动基于此路径生成）
DATABASE_DIR=/app/db

# 文件上传配置
MAX_CONTENT_LENGTH=104857600
# 文件上传目录：
# - 本地开发：使用相对路径 uploads
# - Docker环境：使用绝对路径 /app/uploads（Docker Compose会自动处理）
UPLOAD_FOLDER=uploads
ENABLE_FILE_TYPE_CHECK=false

# 分享链接配置
DEFAULT_SHARE_DAYS=7
SHARE_HOST=http://localhost:5000

# 安全配置
SECRET_KEY=dev-secret-key-change-in-production 